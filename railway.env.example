# Railway Environment Variables for Kinect
# These will be set in Railway's dashboard during deployment

# ============================================
# REQUIRED: Database Connection
# ============================================
# Railway will auto-generate this when you add MongoDB
DATABASE_URL=mongodb://mongo:27017/kinect

# Alternative: Use Railway's MongoDB service
# MONGODB_URI=${{MongoDB.DATABASE_URL}}

# ============================================
# REQUIRED: Security Keys (Auto-generate these!)
# ============================================
# Use Railway's built-in secret generator for these
JWT_SECRET=${{Railway.RAILWAY_STATIC_URL}}-jwt-secret
JWT_REFRESH_SECRET=${{Railway.RAILWAY_STATIC_URL}}-refresh-secret  
DATA_ENCRYPTION_KEY=${{Railway.RAILWAY_STATIC_URL}}-encryption-key

# ============================================
# APPLICATION SETTINGS
# ============================================
NODE_ENV=production
PORT=${{Railway.PORT}}
FRONTEND_URL=https://${{Railway.RAILWAY_STATIC_URL}}
CORS_ORIGIN=https://${{Railway.RAILWAY_STATIC_URL}}

# Self-hosted configuration
SELF_HOSTED=true
ENABLE_TELEMETRY=false
ENABLE_ANALYTICS=false
ALLOW_REGISTRATION=true

# ============================================
# EMAIL CONFIGURATION (Required for reminders)
# ============================================
# Gmail configuration (recommended for Railway)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
SMTP_FROM=Kinect Reminders <your-email@gmail.com>

# SendGrid alternative (good for Railway)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587  
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=Kinect <noreply@yourdomain.com>

# ============================================
# REMINDER SETTINGS
# ============================================
REMINDER_CRON_ENABLED=true
REMINDER_CHECK_INTERVAL=0 9 * * *
ENABLE_EMAIL_REMINDERS=true

# ============================================
# RAILWAY-SPECIFIC SETTINGS
# ============================================
# These are automatically set by Railway
RAILWAY_DEPLOYMENT=true
RAILWAY_ENVIRONMENT_NAME=${{Railway.RAILWAY_ENVIRONMENT_NAME}}
RAILWAY_PROJECT_NAME=${{Railway.RAILWAY_PROJECT_NAME}}

# ============================================
# OPTIONAL: Custom Domain
# ============================================
# If you connect a custom domain in Railway
# CUSTOM_DOMAIN=kinect.yourdomain.com
# FRONTEND_URL=https://kinect.yourdomain.com
# CORS_ORIGIN=https://kinect.yourdomain.com