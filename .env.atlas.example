# MongoDB Atlas Configuration Example
# Copy this to .env and update with your actual values

# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://kinect-admin:YOUR_PASSWORD_HERE@cluster0.xxxxx.mongodb.net/kinect?retryWrites=true&w=majority

# Node Environment
NODE_ENV=development

# Server Port
PORT=3001

# JWT Secrets (generate secure random strings for production)
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production

# CORS Configuration (update for production)
CORS_ORIGIN=http://localhost:5173

# Email Service (optional for local testing)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-mailtrap-user
SMTP_PASS=your-mailtrap-pass
FROM_EMAIL=noreply@kinect.local

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173